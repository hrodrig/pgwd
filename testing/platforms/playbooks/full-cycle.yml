---
# full-cycle.yml — preflight → setup → test → teardown (release validation).
- name: "pgwd full cycle: preflight"
  ansible.builtin.import_playbook: preflight.yml

- name: "pgwd full cycle: setup"
  ansible.builtin.import_playbook: setup.yml

- name: "pgwd full cycle: test"
  ansible.builtin.import_playbook: test.yml

- name: "pgwd full cycle: teardown"
  ansible.builtin.import_playbook: teardown.yml
