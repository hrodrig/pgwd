# Postgres for pgwd e2e test (kind cluster).
# Same credentials as testing/compose.yaml: pgwd/pgwd/pgwd
---
apiVersion: v1
kind: Namespace
metadata:
  name: pgwd-e2e
---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: pgwd-e2e
type: Opaque
stringData:
  password: pgwd
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: pgwd-e2e
  labels:
    app: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: Always
          env:
            - name: POSTGRES_USER
              value: pgwd
            - name: POSTGRES_PASSWORD
              value: pgwd
            - name: POSTGRES_DB
              value: pgwd
          ports:
            - containerPort: 5432
          securityContext:
            allowPrivilegeEscalation: false
          resources:
            limits:
              memory: 1Gi
              cpu: "0.5"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "pgwd", "-d", "pgwd"]
            initialDelaySeconds: 5
            periodSeconds: 2
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "pgwd", "-d", "pgwd"]
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: pgwd-e2e
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres2
  namespace: pgwd-e2e
  labels:
    app: postgres2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres2
  template:
    metadata:
      labels:
        app: postgres2
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: Always
          env:
            - name: POSTGRES_USER
              value: pgwd
            - name: POSTGRES_PASSWORD
              value: pgwd
            - name: POSTGRES_DB
              value: analytics
          ports:
            - containerPort: 5432
          securityContext:
            allowPrivilegeEscalation: false
          resources:
            limits:
              memory: 1Gi
              cpu: "0.5"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "pgwd", "-d", "analytics"]
            initialDelaySeconds: 5
            periodSeconds: 2
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "pgwd", "-d", "analytics"]
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: postgres2
  namespace: pgwd-e2e
spec:
  selector:
    app: postgres2
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres3
  namespace: pgwd-e2e
  labels:
    app: postgres3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres3
  template:
    metadata:
      labels:
        app: postgres3
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: Always
          env:
            - name: POSTGRES_USER
              value: pgwd
            - name: POSTGRES_PASSWORD
              value: pgwd
            - name: POSTGRES_DB
              value: replica
          ports:
            - containerPort: 5432
          securityContext:
            allowPrivilegeEscalation: false
          resources:
            limits:
              memory: 1Gi
              cpu: "0.5"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "pgwd", "-d", "replica"]
            initialDelaySeconds: 5
            periodSeconds: 2
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "pgwd", "-d", "replica"]
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: postgres3
  namespace: pgwd-e2e
spec:
  selector:
    app: postgres3
  ports:
    - port: 5432
      targetPort: 5432
